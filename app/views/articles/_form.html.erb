<%= form_with(model: article, local: true, class: "text-center p-2 mt-3 mb-4 col mx-auto bg-light") do |form| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field my-sm-5">
  <div class="row">
    <%= form.label :title, "TITLE", class: "offset-2" %>
    </div>
  <div class="row">
    <%= form.text_field :title, class: "col-6 col-sm-4 mb-3 offset-2 border" %>
  </div>
  </div>

  <div class="field my-sm-5">
    <div class="row">
    <%= form.label :link, "LINK", class: "offset-2" %>
    </div>
      <div class="row">
    <%= form.text_field :link, class: "col-8 col-sm-6 mb-3 offset-2 border" %>
  </div>
  </div>

  <div class="field my-sm-5">
  <div class="row">
    <%= form.label :text, "TEXT", class: "offset-2" %>
  </div>
    <div class="row">
    <%= form.text_area :text, class: "col-8 col-sm-6 mb-3 offset-2 border" %>
  </div>
  </div>

  <div class="actions col-5 col-sm-2 mx-auto my-3 my-sm-5">
    <%= form.submit "投稿", class: "text-white bg-secondary"%>
  </div>
<% end %>
